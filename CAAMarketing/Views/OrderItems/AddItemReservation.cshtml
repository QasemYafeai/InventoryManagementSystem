@model CAAMarketing.Models.ItemReservation
@inject IHttpContextAccessor HttpContextAccessor

@{
    ViewData["Title"] = "Create";
}

@{
    HttpContextAccessor.HttpContext.Session.SetString("SingleEventBooking", "True");
    HttpContextAccessor.HttpContext.Session.SetString("ItemIDFromSingleEventBooking", @Model.ItemId.ToString());
    //<p>This is the ItemName: @Model.ItemId</p>
}
<partial name="_addEventModal" />
<form asp-action="AddItemReservation">
    <input type="hidden" name="ItemID" value="@Model.ItemId" />
    <input type="hidden" name="ItemName" value="@ViewData["ItemName"]" />
    <div class="cont">
        <div class="deimage">
                <h1 style="text-align: center;">Create new Reservation</h1>


            
            
            <small style="color: red">* required fields</small>

            <hr />
            <div class="deimage2" style="text-align:left;">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>


                <div class="form-group">
                    <label class="control-label required-label">Event Name:</label>
                    <input name="EventName" class="form-control" value="" placeholder="ScotiaBank Arena Seasonal Fair" required/>
                </div>

                <div class="form-group">
                    <label class="control-label">Event Description:</label>
                    <textarea name="EventDescr" class="form-control" placeholder="Celebrate events for the community" rows="3" ></textarea>
                </div>

                <div class="form-group">
                    <label class="control-label required-label">Event Location/Address</label>
                    <input name="EventLocation" class="form-control" value="" placeholder="Niagara Falls" required/>
                </div>
                

@*
                <div class="form-group">
                    <label asp-for="Event.Description" class="control-label required-label"></label>
                    <input asp-for="Event.Description" class="form-control" required id="EventDescription"/>
                    <span asp-validation-for="Event.Description" class="text-danger"></span>
                </div>


                <div class="form-group">
                    <label asp-for="Event.location" class="control-label required-label"></label>
                    <input asp-for="Event.location" class="form-control" required id="eventLocation"/>
                    <span asp-validation-for="Event.location" class="text-danger"></span>
                </div>*@


    @*          <div class="form-group">
                    <label class="control-label required-label">Choose Event</label>
                    <div class="input-group">
                        <select asp-for="EventId" class="form-control select2" asp-items="ViewBag.EventId" required>
                            <option value="">None</option>
                        </select>
                        <button class="btn btn-plus" id="nowAddEvent" title="Add Event" data-bs-toggle="modal" data-bs-target="#addEventModal" type="button"></button>
                    </div>
                </div>*@
                @*                <div class="form-group" hidden= "">
                <label asp-for="ItemId" class="control-label"></label>
                <select asp-for="ItemId" class="form-control select2" asp-items="ViewBag.ItemId">
                <option value="">None</option>
                </select>
                </div>*@
                <div class="form-group">
                    <label asp-for="LocationID" class="control-label required-label"></label>
                    <select asp-for="LocationID" class="form-control" asp-items="ViewBag.LocationID" id="fromLocationDropdown" required>
                        <option value="" disabled selected>Please choose</option>
                    </select>
                    @*<input type="hidden" asp-for="Quantity"/>*@
                    <label id="quantityText"></label>
                </div>
                <div class="form-group">
                    <label asp-for="Quantity" class="control-label required-label"></label>
                    <input asp-for="Quantity" class="form-control" required />
                    <span asp-validation-for="Quantity" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="ReservedDate" class="control-label required-label"></label>
                    <input asp-for="ReservedDate" class="form-control" value="@DateTime.Today.ToString("yyyy-MM-dd")" required />
                    <span asp-validation-for="ReservedDate" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="ReturnDate" class="control-label required-label"></label>
                    <input asp-for="ReturnDate" class="form-control" value="@DateTime.Today.ToString("yyyy-MM-dd")" required />
                    <span asp-validation-for="ReturnDate" class="text-danger"></span>
                </div>
               
            </div>
            <div style="margin-top:40px">
                <input type="submit" title="Click here to create new Reservations" value="Create" class="btn-transfer" />
                <a asp-action="AddMoreItemsEvents" title="Click here to Add more items to the event" class="btn-transfer">Add More Items</a>
                <div class="tooltip"> <a asp-action="Index"> <img src="~/images/icons8-go-back-24.png" alt="Back Icon" /> <span class="tooltiptext">Back</span></a> </div>
            </div>
        </div>
    </div>
</form>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }


    <script src="~/js/refreshDDL.js"></script>


    <script type="text/javascript">
        $('#submitEventCreate').click(function (event) {
            event.preventDefault();
            var form = $('#createEventForm');
            if (form.valid()) {
                $.ajax({
                    type: "POST",
                    url: form.attr('action'),
                    data: form.serialize(),
                    success: function () {
                        $('#addEventModal').modal('hide');
                    },
                    error: function () {
                        alert("Create Failed.  There was a problem adding the record.");
                    }
                });
            }
            return false;
        });
        $('#addEventModal').on('hide.bs.modal', function () {
            $('#createEventForm').find("input[type=text], textarea, select").val("");//to clear the data in the popup
            refreshDDL('EventId', '/ItemReservations/GetEvents', false, null, true, 'None', true);
        });
    </script>

    <link href="https://cdnjs.cloudflare.com/ajax/libs/select2/4.0.13/css/select2.min.css" rel="stylesheet" />
    <script src="https://cdnjs.cloudflare.com/ajax/libs/select2/4.0.13/js/select2.min.js"></script>
    <script>
        $(document).ready(function () {
            $('.select2').select2();
        });

        $(function () {
            // Remove select2 plugin from event dropdown when add event button is clicked
            $('#nowAddEvent').on('click', function () {
                $('select[name="EventId"]').select2('destroy');
            });

            $('#nowAddEvent').on('click', function () {
                $('select[name="ItemId"]').select2('destroy');
            });

            // Add select2 plugin back to event dropdown when add event modal is hidden
            $('#addEventModal').on('hidden.bs.modal', function () {
                $('select[name="EventId"]').select2();
            });

            // Add select2 plugin back to event dropdown when add event modal is hidden
            $('#addEventModal').on('hidden.bs.modal', function () {
                $('select[name="ItemId"]').select2();
            });
        });
    </script>
}